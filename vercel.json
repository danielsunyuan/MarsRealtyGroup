{
  "version": 2,
  "builds": [
    {
      "src": "MarsRealtyGroup/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/[^.]+", "dest": "/index.html" }
  ]
}

